<?xml version="1.0"?>
<block>
  <name>trickle_sink</name>
  <key>trickling_items_trickle_sink</key>
  <category>trickling_items</category>
  <import>import trickling_items</import>
  <make>trickling_items.trickle_sink()</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>in</name>
    <type>byte</type>
  </sink>
</block>
